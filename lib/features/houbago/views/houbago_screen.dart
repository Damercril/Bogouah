import 'package:flutter/material.dart';
import 'houbago_admin_screen.dart';

/// Écran principal Houbago - Affiche l'interface admin avec statistiques du programme de parrainage
class HoubagoScreen extends StatelessWidget {
  const HoubagoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const HoubagoAdminScreen();
  }
}
